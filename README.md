
  # roma collection

  This is a code bundle for roma collection. The original project is available at https://www.figma.com/design/5XtQrcsePirQGLuaI9ICiz/roma-collection.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  